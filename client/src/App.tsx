import React, { FC } from "react";
import Header from "./components/Header";
import Form from "./components/Form";

const App: FC = () => {
  return (
    <div>
      <Header></Header>
      <Form></Form>
    </div>
  );
};

export default App;
